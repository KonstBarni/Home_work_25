#pragma once
#include "Trie.h"
#include <vector>
#include <string>

const std::string& inputMess(TrieNode* dict);
const std::string& choiceWord(std::vector<std::string> words);
const std::string& fullMess();